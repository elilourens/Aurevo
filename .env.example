# Copy to .env.local and fill in.
# Google Apps Script web-app URL that receives waitlist signups
# (setup steps in scripts/waitlist-apps-script.gs). Public by design:
# it is inlined into the client bundle.
NEXT_PUBLIC_WAITLIST_WEBHOOK_URL=
